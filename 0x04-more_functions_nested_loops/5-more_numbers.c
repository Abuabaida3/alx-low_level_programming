#include "main.h"
/**
 * more numbers - cheks for cheks for a digit (0 through 9).
 * Return: Always 0.
 */
void more_numbers 
{
	int a;  b'

	for (b = 0; a< 10; a++)
{
	for (b = 0; b < 15; b++)
	{
		if (b/10 != 0)
	{
	_putchar(b / 10 + '10');
	}
             _putchar(b % 10 + '0');
	}
	_putchar('\n');
}
}
                                                   }
