#include "main.h"

/**
* _isupper - func check isuppercase letter
* @c: char to check
* Rwturn: if upper rturn 1, else return 0
*/
int _isupper(int c)
{
if (c >= 'A', && c <= 'Z')
return (1);

return (0);
}
